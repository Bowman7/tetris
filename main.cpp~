#include"raylib.h"

int main(){
  InitWindow(300,300,"window");
  SetTargetFps(60);


  while(!WindowShouldClose()){

    //draw
    BeginDrawing();
    ClearBackground(BLUE);


    EndDrawing();
  }

  return 0;
}
